<h1><%= @artist.name %></h1>
<ul>
<% @artist.songs.each do |song| %>
  <li><%=link_to song.title, song_path(song)%></li>
<% end %>
</ul>

<p>Song count: <%= pluralize(@artist.songs.count, "song") %></p>
